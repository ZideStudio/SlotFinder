import { Canvas, Controls, Meta } from '@storybook/addon-docs/blocks';
import * as SpinnerStories from './Spinner.stories';

<Meta of={SpinnerStories} />

# Spinner

A reusable, accessible Spinner atom.

## Usage

```tsx
<Spinner />
```

## Props

<table>
  <thead>
    <tr>
      <th>Name</th> <th>Type</th> <th>Required</th> <th>Description</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>
        <code>className</code>
      </td>
      <td>
        <code>string</code>
      </td>
      <td>No</td> <td>Custom class for styling</td>
    </tr>
  </tbody>
</table>

## Accessibility

- It uses role="status" to politely announce the loading state to assistive technologies, and aria-label to provide a human-readable description (e.g. "Loading...").

## Example

```tsx
<Spinner />
```

## Playground

<Canvas of={SpinnerStories.Default} sourceState="shown" />
<Controls of={SpinnerStories.Default} />
