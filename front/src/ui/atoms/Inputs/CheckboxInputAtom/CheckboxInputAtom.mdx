import { Canvas, Controls, Meta } from '@storybook/addon-docs/blocks';
import * as CheckboxInputAtomStories from './CheckboxInputAtom.stories';

<Meta of={CheckboxInputAtomStories} />

# TextInCheckboxInputAtom

A reusable, accessible text checkbox atom for forms and UI elements.

## Usage

```tsx
<CheckboxInputAtom />
```

## Props

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Type</th>
      <th>Description</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>
        <code>className</code>
      </td>
      <td>
        <code>string</code>
      </td>
      <td>Custom class for styling</td>
    </tr>
    <tr>
      <td>
        <code>...props</code>
      </td>
      <td>
        <code>input props (HTMLInput)</code>
      </td>
      <td>All standard input props (e.g. value, onChange)</td>
    </tr>
  </tbody>
</table>

## Accessibility

- Follows WCAG 2.2 AA and RGAA 4 guidelines.
- Keyboard accessible.

## Example

```tsx
<CheckboxInputAtom />
```

## Playground

<Canvas of={CheckboxInputAtomStories.Default} sourceState="shown" />
<Controls of={CheckboxInputAtomStories.Default} />
