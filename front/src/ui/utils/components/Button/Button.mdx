import { Canvas, Controls, Meta } from '@storybook/addon-docs/blocks';
import * as ButtonStories from './Button.stories';

<Meta of={ButtonStories} />

# Button

A reusable, accessible Button component for interaction.

## Usage

```tsx
<Button>
  Button content
</Button>

<Button className="custom-class" icon={GoogleIcon}>
  Button content with custom class
</Button>
```

## Props

<table>
  <thead>
    <tr>
      <th>Name</th> <th>Type</th> <th>Required</th> <th>Description</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>
        <code>className</code>
      </td>
      <td>
        <code>string</code>
      </td>
      <td>No</td>
      <td>Custom class for styling</td>
    </tr>
    <tr>
      <td>
        <code>children</code>
      </td>
      <td>
        <code>ReactNode</code>
      </td>
      <td>
        <strong>Yes</strong>
      </td>
      <td>Content displayed inside the Button</td>
    </tr>
  </tbody>
</table>

## Example

```tsx
import GoogleIcon from '@Front/assets/svg/google_icon.svg?react';

<Button>
  Button content
</Button>

<Button className="custom-class" icon={GoogleIcon}>
  Button content with custom class
</Button>

```

## Playground

<Canvas of={ButtonStories.Default} sourceState="shown" />
<Controls of={ButtonStories.Default} />
